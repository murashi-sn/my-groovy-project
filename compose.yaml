services:
  db:
    image: mysql
    restart: always
    volumes:
      - ./initdb:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: sample
    ports:
      - "3306:3306"
